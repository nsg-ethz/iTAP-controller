package ch.ethz.tik.utils;

import ch.ethz.tik.sdnobfuscation.ObfuscationMaskManager;
/*
public class EndpointIdentifier {
	private int ip;
	private long mac;
	private int port;
	
	
	@Override
	public int hashCode() {
		final int prime = 31;
		int result = 1;
		result = prime * result + ip;
		result = prime * result + (int) (mac ^ (mac >>> 32));
		result = prime * result + port;
		return result;
	}
	
	@Override
	public boolean equals(Object obj) {
		if (this == obj)
			return true;
		if (obj == null)
			return false;
		if (getClass() != obj.getClass())
			return false;
		EndpointIdentifier other = (EndpointIdentifier) obj;
		if (ip != other.ip)
			return false;
		if (mac != other.mac)
			return false;
		if (port != other.port)
			return false;
		return true;
	}

	public int getIp() {
		return ip;
	}

	public void setIp(int ip) {
		this.ip = ip;
	}

	public long getMac() {
		return mac;
	}

	public void setMac(long mac) {
		this.mac = mac;
	}

	public int getPort() {
		return port;
	}

	public void setPort(int port) {
		this.port = port;
	}
	
	
    
}*/